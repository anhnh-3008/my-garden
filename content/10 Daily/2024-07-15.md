---
title: 🌱 Daily Note 2024-07-15
date: 2024-07-15
draft: true
---

---

> He can who thinks he can, and he can't who thinks he can't. This is an inexorable, indisputable law.
> — <cite>Pablo Picasso</cite>

![photo by Lynn Van den Broeck on Unsplash](https://images.unsplash.com/photo-1613132061374-700e1ddd0c86?crop=entropy&cs=srgb&fm=jpg&ixid=M3wzNjM5Nzd8MHwxfHJhbmRvbXx8fHx8fHx8fDE3MjEwMTYwOTB8&ixlib=rb-4.0.3&q=85&w=500&h=500)

---

# 💼 Work
---
- [ ] Task description


# 🌱 Personal
---
## Refactor đoạn code của anh Tuấn
### 1. Vấn đề
1. Type của video đang set kiểu số => code khó đọc
2.  Rescue error - cần xem thêm
3. Method purchase dài => khó đọc, maintain và phát triển

### 2. Ý tưởng
1. Tách phần tính giá thuê video ra thành một phương thức riêng trong lớp `RentalCart`. Điều này giúp tách biệt logic tính giá thuê video và logic tạo hóa đơn.
2. Sử dụng hằng số hoặc enum thay vì sử dụng số cứng để đại diện cho loại video. Điều này giúp mã nguồn dễ đọc và dễ hiểu hơn.
3. Sử dụng phương thức `map` để tính tổng số tiền và số điểm thưởng thay vì sử dụng vòng lặp `each`.
4. Sử dụng phương thức `join` để tạo chuỗi hóa đơn thay vì sử dụng toán tử `+=`.
5. Sử dụng biểu thức điều kiện ngắn gọn (`ternary operator`) để rút gọn logic tính giá thuê video.
6. 1. Tách phần logic tính giá tiền và tính điểm thành các phương thức riêng trong lớp RentalCart. Điều này giúp tách biệt các trách nhiệm và làm cho code dễ đọc hơn.
    
2. Sử dụng hằng số để định nghĩa các loại video thay vì sử dụng enum. Điều này giúp tăng tính rõ ràng và dễ bảo trì của code.
    
3. Sử dụng phương thức getter và setter thay vì attr_accessor để giới hạn quyền truy cập vào các thuộc tính của lớp.
    
4. Sử dụng biến cục bộ thay vì biến instance trong phương thức calculate_point để tính điểm. Điều này giúp tránh sự nhầm lẫn và làm cho code dễ hiểu hơn.
    
	1. Sử dụng interpolation để tạo chuỗi receipt thay vì cộng chuỗi bằng phép toán +. Điều này giúp tăng tính rõ ràng và dễ đọc của code.
1. Sử dụng enum thay vì hash constant: Thay vì sử dụng hash constant `TYPES`, hãy sử dụng enum để định nghĩa các loại video. Điều này giúp tăng tính rõ ràng và dễ đọc của code.
    
2. Tách logic tính giá thành phần riêng: Tách logic tính giá của từng loại video thành các phương thức riêng trong class `Video`. Điều này giúp tách biệt logic và tăng tính mở rộng của code.
    
3. Sử dụng strategy pattern cho tính giá: Sử dụng strategy pattern để tách logic tính giá thành các strategy riêng biệt. Mỗi loại video sẽ có một strategy tính giá tương ứng. Điều này giúp giảm sự phụ thuộc và tăng tính linh hoạt của code.
    
4. Sử dụng factory pattern cho việc tạo đối tượng: Sử dụng factory pattern để tạo đối tượng `Video` và `Rental`. Factory pattern giúp tách biệt logic tạo đối tượng và tăng tính mở rộng của code.
    
5. Sử dụng template method pattern cho việc tính điểm: Sử dụng template method pattern để tách logic tính điểm thành các bước riêng biệt. Mỗi loại video sẽ có một template method tính điểm tương ứng. Điều này giúp giảm sự phụ thuộc và tăng tính linh hoạt của code.
    
6. Sử dụng builder pattern cho việc tạo `RentalCart`: Sử dụng builder pattern để tạo đối tượng `RentalCart`. Builder pattern giúp tạo đối tượng `RentalCart` một cách linh hoạt và dễ dàng.
    
7. Sử dụng dependency injection: Thay vì khởi tạo các đối tượng `Video` và `Rental` trong `RentalCart`, hãy sử dụng dependency injection để chuyển các đối tượng này vào `RentalCart`. Điều này giúp giảm sự phụ thuộc và tăng tính linh hoạt của code.
    
8. Sử dụng các tên biến và phương thức rõ ràng: Đặt tên biến và phương thức sao cho rõ ràng và mô tả được ý nghĩa của chúng. Điều này giúp tăng tính đọc và hiểu code.
    

Đây chỉ là một số ý tưởng để refactor code. Bạn có thể áp dụng một hoặc nhiều ý tưởng này để cải thiện code của bạn.Dưới đây là một số ý tưởng để refactor code theo chuẩn SOLID, OOP và design patterns:

1. Sử dụng enum thay vì hash constant: Thay vì sử dụng hash constant `TYPES`, hãy sử dụng enum để định nghĩa các loại video. Điều này giúp tăng tính rõ ràng và dễ đọc của code.
    
2. Tách logic tính giá thành phần riêng: Tách logic tính giá của từng loại video thành các phương thức riêng trong class `Video`. Điều này giúp tách biệt logic và tăng tính mở rộng của code.
    
3. Sử dụng strategy pattern cho tính giá: Sử dụng strategy pattern để tách logic tính giá thành các strategy riêng biệt. Mỗi loại video sẽ có một strategy tính giá tương ứng. Điều này giúp giảm sự phụ thuộc và tăng tính linh hoạt của code.
    
4. Sử dụng factory pattern cho việc tạo đối tượng: Sử dụng factory pattern để tạo đối tượng `Video` và `Rental`. Factory pattern giúp tách biệt logic tạo đối tượng và tăng tính mở rộng của code.
    
5. Sử dụng template method pattern cho việc tính điểm: Sử dụng template method pattern để tách logic tính điểm thành các bước riêng biệt. Mỗi loại video sẽ có một template method tính điểm tương ứng. Điều này giúp giảm sự phụ thuộc và tăng tính linh hoạt của code.
    
6. Sử dụng builder pattern cho việc tạo `RentalCart`: Sử dụng builder pattern để tạo đối tượng `RentalCart`. Builder pattern giúp tạo đối tượng `RentalCart` một cách linh hoạt và dễ dàng.
    
7. Sử dụng dependency injection: Thay vì khởi tạo các đối tượng `Video` và `Rental` trong `RentalCart`, hãy sử dụng dependency injection để chuyển các đối tượng này vào `RentalCart`. Điều này giúp giảm sự phụ thuộc và tăng tính linh hoạt của code.
    
8. Sử dụng các tên biến và phương thức rõ ràng: Đặt tên biến và phương thức sao cho rõ ràng và mô tả được ý nghĩa của chúng. Điều này giúp tăng tính đọc và hiểu code.
    

Đây chỉ là một số ý tưởng để refactor code. Bạn có thể áp dụng một hoặc nhiều ý tưởng này để cải thiện code của bạn.
## 3. Giải pháp
1. Type của video đang set kiểu số
	- Sửa lại dùng enum cho dễ đọc.
2. Rescue error

3. Method purchase cồng kềnh
	- Tách logic tính tiền ra 1 method
	- Tách logic tạo receipt ra 1 method
	- 



### 4. Báo cáo

- Nghiệp vụ:
	- Xử lý logic tạo hóa đơn cho dịch vụ thuê băng đĩa
	- Thời gian thuê quá 30 ngày => raise error(? - e chưa hiểu nghiệp vụ đoạn này lắm, nhưng có thể xử lý thêm error message, custom lại phần rescue exception, nhưng do không hiểu nghiệp vụ nên e tạm skip)
	- Hóa đơn xuất ra sẽ bao gồm thông tin của tất cả băng/đĩa đã được thuê(giá tiền, số ngày thuê), tổng tiền thuê, điểm được tích.
	- Giá tiền sẽ tùy thuộc vào loại video, số ngày thuê và chương trình khuyến mại
- Refactor
	- Thêm `# frozen_string_literal: true` để tối ưu bộ nhớ, cải thiện performance khi biên dịch.
	- Sửa lại tên biến cho phù hợp, vd total_amount -> total_cost, this_amount -> this_cost
	- Sửa lại type của video thành enum để code dễ đọc hơn, thay vì sử dụng số.
	- Tách logic tính tiền và tính điểm ra một method riêng.
	- Áp dụng Factory Pattern để xử lý logic tính tiền. Dễ maintain cũng như mở rộng code.(hỏi Copilot gợi ý các design patterns có thể áp dụng để refactor, nó rcm cho e 2 DP là strategy và factory, e chọn factory vì e thấy dễ hiểu hơn).
	- Tương tự có thể áp dụng pattern trên với logic tính điểm.
- Link refactor: 